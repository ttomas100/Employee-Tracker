import { Dictionary } from '../models/common';
import { Column, Row } from '../models/internal-table';
import { ALIGNMENT, COLOR } from './table-constants';
export interface RowOptionsRaw {
    color: string;
}
export interface RowOptions {
    color: COLOR;
}
export declare function convertRawRowOptionsToStandard(options?: RowOptionsRaw): RowOptions | undefined;
export declare function textWithPadding(text: string, alignment: ALIGNMENT, mxColumnLen: number): string;
export declare function createTableHorizontalBorders({ left, mid, right, other, }: {
    left: string;
    mid: string;
    right: string;
    other: string;
}, column_lengths: number[]): string;
export declare function createColum(name: string): Column;
export declare function createRow(color: COLOR, text: Dictionary): Row;
export declare function findMaxLenOfColumn(column: Column, rows: Row[]): number;
export declare function renderTableHorizontalBorders(style: any, column_lengths: number[]): string;
export declare function createHeaderAsRow(createRowFn: any, columns: Column[]): Row;
export declare function cellText(text: string): string;
